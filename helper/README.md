# Owlet Cam helper

This directory is source/build infrastructure for the future isolated native
helper. Milestone 0 contains no helper implementation or compiled artefact.
Compiled helpers must be produced as versioned release assets and must never
include Owlet applications, proprietary libraries, SDK keys, or camera/account
credentials.
