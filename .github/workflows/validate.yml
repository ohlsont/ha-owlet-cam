name: Validate

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  hacs:
    if: github.event.repository.private == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: hacs/action@main
        with:
          category: integration
          repository: ${{ github.repository }}

  hacs-private-readiness:
    if: github.event.repository.private
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - name: Record public-only HACS gate
        run: >-
          echo 'HACS Action is intentionally deferred while this repository is
          private; it remains configured as a required public-repository gate.'

  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: home-assistant/actions/hassfest@master

  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          python-version: "3.14"
          enable-cache: true
      - run: uv sync --all-groups
      - run: uv run ruff format --check .
      - run: uv run ruff check .
      - run: uv run mypy custom_components scripts
      - run: uv run python scripts/validate_release.py
      - run: uv run python scripts/check_secrets.py
      - name: Build and inspect standalone desktop preparer
        run: |
          uv run python scripts/build_preparer_zipapp.py \
            --output /tmp/owlet-cam-prepare.pyz
          uv run python /tmp/owlet-cam-prepare.pyz --help
          uv run python scripts/inspect_release_assets.py \
            /tmp/owlet-cam-prepare.pyz
      - name: Validate JSON
        run: >-
          uv run python -c "import json,pathlib;
          [json.loads(p.read_text()) for p in pathlib.Path('.').rglob('*.json')];
          print('JSON valid')"
